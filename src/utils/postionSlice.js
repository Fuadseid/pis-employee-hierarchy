import { createSlice } from "@reduxjs/toolkit";
const initialState = {};
const postionslice = createSlice({
  initialState,
  reducers: {},
});
export const {} = postionslice.actions;
export default postionslice.reducer;
